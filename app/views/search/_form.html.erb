en el nav
<%= render 'search/form' %>

<div class="left">
  <%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
  <%= f.search_field :title_or_body_or_user_email_or_user_username_i_cont_any,
   placeholder: "Buscar...",
   class: "form-control me-2" %>
  <%= f.submit "Buscar!", class:"btn btn-outline-success" %>
<% end %>


</div>
